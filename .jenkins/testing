pipeline {
  agent { label 'docker' }
  stages {
    stage('Build') {
      steps {
        echo 'Building ...'
        sh 'docker build -t openstax/cnx-automation:dev .'
      }
    }
    stage('Test') {
      steps {
        echo 'Testing ...'
        sh 'make ci-test'
      }
    }
  }
}
