language: python
matrix:
  include:
    - python: 3.6
      env: TOXENV=tests
    - python: 3.6
      env: TOXENV=flake8
addons:
  chrome: stable
env:
  - CHROME_DRIVER_PATH=/usr/bin/google-chrome-stable
install:
  - pip install -U pip
  - pip install tox
script:
  - tox

