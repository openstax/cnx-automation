language: python

sudo: required

services:
  - docker

before_install:
  - docker info

install:
  - docker-compose build
  - docker-compose up -d

script:
  - docker-compose exec --user root selenium-chrome tox

after_success:
  - docker-compose down
